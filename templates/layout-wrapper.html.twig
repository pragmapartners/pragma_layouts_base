{#
/**
 * @file
 * Default implementation for a custom layout bento section.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 * - region_attributes: HTML attributes for the region <div>.
 * - settings: An array of configured settings for the layout.
 *
 * @ingroup themeable
 */
#}
{% set section_classes = [
  'section-wrapper',
] %}

<section{{layout_attributes.addClass(section_classes)}}>
	{% block layout %}
		<div{{sub_attibutes}}>
			{{content}}
		</div>
	{% endblock %}
</section>
